funct example(a, b) {
    A = eye(a, b);
    B = ones(b, a);
    A[1, 1] = 3;
    return A*B;
}

x = example(3, 4);

y = [[4, 3],
     [3, 2]];
print inv(y);

// print f_read(filename);

// for i = 0:f_lines(filename)-1 {
//     print f_readline(filename, i);
// }

funct fibonacci(x) {
    if (x == 0 or x == 1) {
        return 1;
    }
    return fibonacci(x-1) + fibonacci(x-2);
}

print ln(10);
print round(1.123456789, 3);
print sin(3.14159265358979323846264338327950/2);

x = 5;
x %= 2;
print x;

a = 1;
b = 2;
funct foo(b, a) { return b - a; }
print foo(a, b);


i = 0;
funct next() { i += 1; return i < 5; }
while (next()) { print i; }


a = "";
if (a == "") {
    a = "abc";
}
print a;


funct test(a) {
    print a';
}

test([[1, 2], [3, 4]]);