funct example(a, b) {
    A = eye(a, b);
    B = ones(b, a);
    A[1, 1] = 3;
    return A*B;
}

x = example(3, 4);
print type(example);

filename = "../mrix/interpreter/Interpreter.java";

print f_read(filename);

//for i = 0:f_lines(filename)-1 {
//    print f_readline(filename, i);
//}